var frindsAge=[20,21,24,22,23,25];

//1.print with 'node array.js'
    // console.log(frindsAge);

//2.indexes start from '0'
    // console.log("age of Rafi and Nasim: ",frindsAge[0],frindsAge[1]);

//3.taking an element in a variable from the array
    // var karim=frindsAge[3];
    // console.log(karim);

//4.updating an index in array
    // frindsAge[2]=19;
    // console.log("The correct age of abid",frindsAge[2]);//24 to 19

//5.finding the position of an array element
    // var position = frindsAge.indexOf(23);
    // console.log(position);

//6.if the array element doesn't exist
    // var anotherPosition = frindsAge.indexOf(100);
    // console.log(anotherPosition); //shows -1

//7.push any element in the array.value added on the end
    // console.log(frindsAge);

    // frindsAge.push(28);
    // frindsAge.push(27);
    // console.log(frindsAge);

//8.finding array's element/length.here we see both, before adding elements and after added elements.
    // console.log(frindsAge.length);

    // frindsAge.push(28);
    // frindsAge.push(27);
    // console.log(frindsAge.length);


//9.delete or pop an element from the array.like push pop delete from the end
    // console.log(frindsAge);

    // frindsAge.push(28);
    // frindsAge.push(27);
    // console.log(frindsAge);

    // frindsAge.pop();
    // console.log(frindsAge);


//10.add/push elemet on the beginning with 'unshift' function
    // console.log(frindsAge);

    // frindsAge.unshift(29);
    // console.log(frindsAge);

//11.delete/pop elemet on the beginning with 'shift' function
    // console.log(frindsAge);

    // frindsAge.shift();
    // console.log(frindsAge);